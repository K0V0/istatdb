$('section.items_table, section.item_details').remove();

$("section.calculator_list").remove();

<% o = render('layouts/shared/items_table',

	fields: [
		ident: {
			is_sortlink: true,
			is_highlighted: true,
			is_link: {
				remote: true
			}
		},
		description: {
			is_highlighted: true,
			is_multiple_with: :ident
		},
		local_taric: [
			kncode: {
				call_decorator: :num_to_kncode,
				is_link: true
			}
		]
	],

	errors: [
		no_results: {
			partial: "goods/index/no_results"
		}
	], 

	options: [
		head: true,
		foot: true
	]

) %>

$('<%=j o %>').appendTo('main');