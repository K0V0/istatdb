<%= render 'layouts/main_menu' %>

<section class="form">

	<%= form_for @good do |t| %>

		<article>
			<%= render 'goods/edit/good', obj: t %>
		</article>

		<div>
			<%= t.submit class: "button" %>
		</div>

	<% end %>

</section>