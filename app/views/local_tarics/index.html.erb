
<%= render 'layouts/base/main_menu' %>

<%= render 'local_tarics/shared/search' %>

<article class="index local_tarics">

	<%= render('layouts/shared/items_table',

		fields: {
			kncode: {
				call_decorator: :num_to_kncode,
				is_highlighted: true,
				is_link: true
			},
			description: {
				
			}
		},

		#errors: [
		#	no_results: {
		#		partial: "goods/index/no_results"
		#	}
		#], 

		options: [
			head: true,
			foot: true
		]

	) %>
	
</article>