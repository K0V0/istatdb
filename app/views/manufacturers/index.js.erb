elem = $('section.items_table');

elem.remove();

<%
	if @result.length > 0

		o = render('layouts/items_table',

			fields: {
				name: {
					is_sortlink: true,
					is_highlighted: true
				},
				taric_codes: {
					call_function: "num_to_kncode"
				},
				goods_manufacturers_count: {

				},
				intrastat_clients: {

				},
				incoterm_shortlands: {

				} 
			}
		) 

		else %>

		$('<%=j render("layouts/items_table_no_results", items: @result, err_message: :manufacturer_search_no_results) %>')
		.appendTo('main');

	<% end
%>

$('<%=j o %>').appendTo('main');

// remember sort link direction
$("#q_s").val('<%= params[:q][:s] if params.deep_has_key? :q, :s %>');

// remember page number to go back
$("input[name=page]").val("<%=j @MEM.page %>");

if ('<%= @administrative_mode %>' == 'true') {
	GOODS_ACTIONS.refresh();
}

$("#manufacturers").fixHeader();

