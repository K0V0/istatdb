<%= render 'layouts/main_menu' %>

<section class="form">

	<%= form_for @manufacturer, url: update_manufacturer_path do |t| %>

		<article class="decorated_wrap shrinked">

			<%= render('layouts/new_select_section', 
				obj: t,
				input_fields: { 
					name: {
						required: true,
						autocomplete: false
					}
				},
			) %>
			
		</article>

		<article class="decorated_wrap shrinked">
			
			<div class="tablewrap">
			
				<table class="rows_colored new_checkboxes_list with_efects">

					<thead>
						<tr>
							<th colspan="10"> 
								<%= cbt :associated_to_impexpcompany %>
							</th>
						</tr>
					</thead>

					<tbody>
						<%= t.collection_check_boxes(
							:impexpcompany_ids,
							@impexpcompanies_all,
							:id,
							:company_name
						) do |b| 
							("<tr> <td>" +
							b.check_box +
							"</td><td>" +
							b.label +
							"</td></tr>").html_safe 
						end %>
					</tbody>

				</table>
				
			</div>

		</article>

		<article class="decorated_wrap shrinked">

			<div>
				
				<div>
					<b> <%= cbt :edit_assocs_details %> </b>
				</div>

				<br>

				<div>
					<%= t.label :impexpcompany_filter %>
					<div class="select_wrapper">
						<%= select(
							:edit_impexpcompany_manufacturer,
							:impexpcompany_id,
							@impexpcompanies_associated.collect{ |p| [p.company_name, p.id] }
						) %>
						<span></span>
					</div><!--

					--><%= t.button cbt(:edit_manufacturer_details) , name: "edit_attrs", value: "1", class: "button" %>
				</div>

			</div>

			<div class="form_errors"></div>

		</article>

		<article class="decorated_wrap shrinked">
			<div>

				<%= t.submit %>

				<%= link_to t(:back), manufacturers_path, class: "button" %>

			</div>
		</article>

	<% end %>

</section>