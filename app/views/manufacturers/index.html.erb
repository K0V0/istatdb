
<%= render 'layouts/main_menu' %>

<%= render 'manufacturers/manufacturerslist/search' %>

<% if @result.length > 0 %>

	<%= render('layouts/items_table',

		fields: {
			name: {
				is_sortlink: true,
				is_highlighted: true
			},
			taric_codes: {
				
			},
			goods_count: {

			},
			intrastat_clients: {

			}
		}
	) %>

<% else %>

	<%= render(
		'layouts/items_table_no_results',
		items: @result,
		err_message: :manufacturer_search_no_results
	) %>

<% end %>