
<%= render 'layouts/base/main_menu' %>

<%= render 'manufacturers/shared/search' %>

<article class="index manufacturers">
	<%= render('layouts/shared/items_table',

		fields: [
			name: {
				is_sortlink: true,
				is_highlighted: true,
				is_link: {
					remote: true
				}
			},
			intrastat_clients: {

			}
			#description: {
				#is_highlighted: true,
				#is_multiple_with: :ident
			#}
			
		],

		#errors: [
		#	no_results: {
		#		partial: "goods/index/no_results"
		#	}
		#], 

		options: [
			head: true,
			foot: true
		]

	) %>
</article>
