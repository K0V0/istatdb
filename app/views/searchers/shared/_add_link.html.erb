<%= link_to(
    t('actions.add'),
    add_something_path({
        norender_default_jserb: true,
        search_string: params.deep_has_key?(:q, :search_cont) ? params[:q][:search_cont] : ""
    }),
    class: 'button',
    id: 'add_something_path',
    remote: true
) %>