<td>

	<select
		name="settings[<%= name %>]"
		class="settings"
		id="settings_<%= name %>"
	>
		
		<%= get_options_from_options(
			valshash: options[:options],
			setting_name: name
		) %>

	</select>

</td>