
<% 
	if defined? obj
		object = obj.object
	else
		object = assoc
	end
%>

<% fields.each do |field_name, opts| %>
	<% if object.respond_to? :errors %>
		<%= object.errors[field_name].first %>
	<% end %>
<% end %>
