<% 
	klass = "disabled" if opts[1].nil? || !opts[1].collect(&:id).any?
	klass_html = (html_class if defined?(html_class))||("")
%>

<div>
	<%= obj.label opts.first %>
	<div class="select_wrapper <%= "#{klass} #{klass_html}" %>">
		<%= obj.collection_select *opts %>
		<span></span>
	</div>
</div>