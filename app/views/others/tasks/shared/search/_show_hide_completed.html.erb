<div>

    <span>
        <%= obj.label :done_filter %>
    </span>

    <span>
        <%= obj.select(
            :done_filter,
            options_for_select(
                [
                    [t(:is_true), 1],
                    [t(:is_false), 0]
                ],
                params[:q][:done_filter]
            )
        ) %>
    </span>

</div>
