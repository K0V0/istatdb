<%= render(
    layout: 'layouts/shared/search',
    locals: {
        main_fields: {
            name: :name_or_note_cont
        },
        actions: [
            :add,
            :edit,
            :delete,
            :clear_fields,
            :clear_all,
            :administrative
        ]
    }
) do %>

    <%#= render 'goods/shared/search/select_client_dropdown', obj: @s %>

    <div>
        <span>
            <%= @s.label :impexpcompany_filter %>
        </span>
        <span id="search_items_select_impexpcompany">
            <%= render 'layouts/shared/search/multiselect', obj: @impexpcompanies %>
        </span>
    </div>

    <%= render 'others/issues/shared/search/season_select', obj: @s %>

<% end %>