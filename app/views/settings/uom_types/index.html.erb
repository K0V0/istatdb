<%= render 'settings/shared/settings_submenu' %>

<%= render 'layouts/base/main_menu' %>

<%= render 'settings/shared/actions' %>

<article class="index settings uom_types">
	<%= render('layouts/shared/items_table',

		fields: {
			uom_type: {
				
			},
			full_name: {

			},
			description: {
				
			}
		},

		options: [
			head: true,
			foot: true
		]

	) %>
</article>