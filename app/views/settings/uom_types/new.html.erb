<%= render 'settings/shared/settings_submenu' %>

<%= render 'layouts/base/main_menu' %>

<section class="form">

	<%= form_for @record do |t| %>

		<%= new_edit(
			obj: t,
			fields: {
				uom_type: { 
					autofocus: true
				},
				full_name: {

				},
				description: {
					type: :text_area
				}
			}
		) %>

		<%= new_edit(
			obj: t
		) %>

	<% end %>

</section>