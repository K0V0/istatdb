en:

  activerecord:
    models:
      trade_type: Type of trade operations
    attributes:
      trade_type:
        type: Number code of operation
        description: Description of operations

  helpers:
    submit:
      trade_type:
        create: Add trade (movement) type
        update: Update trade type


sk:

  activerecord:
    models:
      trade_type: Typy obchodných operácií
    attributes:
      trade_type:
        type: Číselný kód operácie
        description: Popis

  helpers:
    submit:
      trade_type:
        create: Pridať typ obchodnej operácie
        update: Upraviť obchodnú operáciu

