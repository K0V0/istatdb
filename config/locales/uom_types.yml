en:

  ransack:
    attributes:
      uom_type:
        uom_type: Units of measurement

  activerecord:
    models:
      uom_type: Units of measurement
    attributes:
      uom_type:
        uom_type: Unit
        full_name: Full name
        description: Description
        intrastat_code: Intrastat code

  helpers:
    submit:
      uom_type:
        create: Add new UOM
        update: Update UOM

  uom_types:
    own: "- own -"


sk:

  ransack:
    attributes:
      uom_type:
        uom_type: Merná jednotka

  activerecord:
    models:
      uom_type: Merné jendontky
    attributes:
      uom_type:
        uom_type: Jednotka
        full_name: Celý názov
        description: Popis
        intrastat_code: Intrastat kód

  helpers:
    submit:
      uom_type:
        create: Pridať jednotku
        update: Upraviť jednotku

  uom_types:
    own: "- vlastná -"

