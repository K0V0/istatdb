
apt-get install aptitude
apt-get install mc
apt-get install htop
apt-get install powertop
apt install hddtemp lm-sensors
apt.get install sudo

AUTOLOGIN
=========

sudo systemctl edit getty@tty1.service

do suboru: (myusername nahradit menom)

[Service]
ExecStart=
ExecStart=-/sbin/agetty --noissue --autologin myusername %I $TERM
Type=idle


VPN (hamachi)
=============

wget https://www.vpn.net/installers/logmein-hamachi_2.1.0.203-1_amd64.deb
su
nano /root/.bashrc
pridat: export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
dpkg -i logmein-hamachi_2.1.0.203-1_amd64.deb
hamachi login
hamachi attach email_siete@example.com

prepnut na webe hamachi na typ zariadenia hub


VYPNUTIE REGULACIE PROCESORA
============================

su
nano /etc/default/grub
upravit: GRUB_CMDLINE_LINUX_DEFAULT="quiet splash intel_pstate=disable processor.max_cstate=1 intel_idle.max_cstate=0 idle=poll"
update-grub


nano /etc/default/cpufrequtils
apt-get install cpufrequtils
GOVERNOR="performance"


INSTALACIA RAILS
================

su
apt install git curl libssl-dev libreadline-dev zlib1g-dev \
        autoconf bison build-essential libyaml-dev \
        libreadline-dev libncurses5-dev libffi-dev libgdbm-dev
exit
echo 'export PATH="$HOME/.rbenv/bin:$PATH"' >> ~/.bashrc
echo 'eval "$(rbenv init -)"' >> ~/.bashrc
source ~/.bashrc

curl -sL https://github.com/rbenv/rbenv-installer/raw/master/bin/rbenv-installer | bash -

rbenv install 2.4.0

su
apt install -y curl gnupg2 dirmngr git-core zlib1g-dev build-essential libssl-dev libreadline-dev libyaml-dev libsqlite3-dev sqlite3 libxml2-dev libxslt1-dev libcurl4-openssl-dev software-properties-common libffi-dev

curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -
apt install -y nodejs

curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
apt update

gem install rails -v 4.2.11

npm install -g localtunnel

su
apt-get install ruby-rmagick imagemagick libmagickcore-6.q16-dev 
exit


INSTALACIA APLIKACIE
====================

znemit verziu rails v gemfile aby sedela s nainstalovanou v systeme
bundle update rails

RAILS_ENV=production rake db:migrate
RAILS_ENV=production rake db:seed
RAILS_ENV=production rake assets:clobber
RAILS_ENV=production rake assets:precompile


AUTOSPUSTENIE PO STARTE
=======================

v subore /home/user/.profile pridat:

PATH="${PATH:+${PATH}:}/home/kovo/.rbenv/versions/2.5.0/lib/ruby/gems/2.5.0/gems:/home/kovo/.rbenv/versions/2.5.0/bin"

GEM_PATH="${GEM_PATH:+${GEM_PATH}:}/home/kovo/.rbenv/versions/2.5.0/lib/ruby/gems/2.5.0/gems:/home/kovo/.rbenv/versions/2.5.0/bin"

/home/kovo/www/customs-taric-db-prod/run.sh


AUTOSET PRACOVNEHO ADRESARA
===========================

do /home/user/.bashrc pridat:

cd /home/user/www/aplikacia


ZALOHY NA FIREMNY SERVER
========================

30 4 * * * /bin/bash -l -c 'cd /home/kovo/www/customs-taric-db-prod && PATH="$HOME/.rbenv/shims":"$HOME/.rbenv/bin":"$PATH" RAILS_ENV=production bin/rake exchange_rates:load --silent >> log/cron_log.log 2>&1'

0 4 * * * /bin/bash -l -c 'cd /home/kovo/www/customs-taric-db-prod && PATH="$HOME/.rbenv/shims":"$HOME/.rbenv/bin":"$PATH" RAILS_ENV=production bin/rake backup:database --silent >> log/cron_log.log 2>&1'

10 4 * * * /bin/bash -l -c 'cd /home/kovo/www/customs-taric-db-prod && PATH="$HOME/.rbenv/shims":"$HOME/.rbenv/bin":"$PATH" RAILS_ENV=production bin/rake backup:files --silent >> log/cron_log.log 2>&1'









